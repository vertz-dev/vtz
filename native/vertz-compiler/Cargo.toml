[package]
name = "vertz-compiler"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "3.8.3", features = ["serde-json"] }
napi-derive = "3.5.2"
vertz-compiler-core = { path = "../vertz-compiler-core" }

[build-dependencies]
napi-build = "2.3.1"
